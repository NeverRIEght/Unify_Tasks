<Window x:Class="Unify_Tasks.DialogWindows.NoteEditor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Unify_Tasks.DialogWindows"
        mc:Ignorable="d"
        Title="NoteEditor" Height="450" Width="800"
        Loaded="Window_Loaded"
        WindowStartupLocation="CenterScreen"
        WindowStyle="ToolWindow"
        ResizeMode="NoResize">
    <Grid Background="{DynamicResource DarkI}">
        <Grid.RowDefinitions>
            <RowDefinition Height="0.8*"/>
            <RowDefinition Height="0.2*"/>
        </Grid.RowDefinitions>

        <RichTextBox
            x:Name="NoteText"
            Foreground="White"
            Background="{DynamicResource MediumI}"
            Margin="5">
        </RichTextBox>

        <Border
            Grid.Row="1"
            Width="200px"
            Height="50px"
            Background="{DynamicResource MediumI}"
            CornerRadius="10">

            <Button
                BorderThickness="0"
                Background="Transparent"
                Foreground="White"
                FontWeight="Medium"
                FontSize="24"
                Content="Save changes"
                Click="Button_Click" />

        </Border>
        
    </Grid>
</Window>
